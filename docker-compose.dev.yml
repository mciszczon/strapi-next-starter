version: "3.7"

x-network:
  &network
  networks:
    - investoko_network

services:

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    volumes:
      - "./frontend/app:/app"
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=development
    links:
      - backend
    command: "next dev"
    <<: *network

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    volumes:
      - "./backend:/app"
    ports:
      - "8000:80"
    command: "/start-reload.sh"
    <<: *network

networks:
  investoko_network:
    external: true

