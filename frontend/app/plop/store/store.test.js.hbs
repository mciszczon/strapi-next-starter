import {{pascalCase name}} from './{{pascalCase name}}'

describe('{{pascalCase name}} Store', () => {
  it('instantiates without error', () => {
    const store = new {{pascalCase name}}()
    expect(store).toBeDefined()
  })
})
